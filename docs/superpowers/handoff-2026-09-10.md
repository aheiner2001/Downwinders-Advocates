# Pipeline handoff — 2026-09-10

## Done

- Eleventy static-site pipeline for Phases 5–8
- **48 HTML pages:** 12 static shells + 19 Covered Areas + 17 Conditions
- Root `_site/robots.txt` (allows GPTBot, ClaudeBot, PerplexityBot, Google-Extended)
- Root `_site/llms.txt` (from handoff reference)
- `_site/sitemap.xml` with 48 URLs
- Slash-slug flattening with tracked report (see below)
- Shared layout: head SEO, OG/Twitter, footer disclaimer, `/check` + `/free-help` links
- `/check` client-side screener ported from reference HTML
- `/contact` three-field shell (Lawmatics deferred)

## Placeholder / deferred

- Shell page body copy (`/is-this-real`, `/siblings`, etc.)
- Live Lawmatics embed on `/contact`
- CMS `Body` rich text (empty by design until legal source copy)

## SEO flag for Jaxon

County slugs containing `/` were flattened. Confirm this URL plan:

See `slug-flatten-report.txt` (also generated on every build). Examples:

- `arizona/coconino-county` → `/covered-areas/arizona-coconino-county/`
- `nevada/clark-county` → `/covered-areas/nevada-clark-county/`

## How to build

```bash
cd .worktrees/static-site-pipeline   # or this branch checkout
npm install
npm run build
npm run verify
```

Output directory: `_site/` (ready for GitHub Pages).

## Branch

`feature/static-site-pipeline`
