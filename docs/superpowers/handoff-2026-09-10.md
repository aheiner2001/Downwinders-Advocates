# Pipeline handoff — 2026-09-10 (strict-copy restart)

## Done

- Eleventy static-site pipeline for Phases 5–8 on branch `feature/strict-copy-pipeline`
- Worktree: `.worktrees/strict-copy-pipeline`
- **48 HTML pages:** 12 static + 19 Covered Areas + 17 Conditions
- Root `_site/robots.txt` (allows GPTBot, ClaudeBot, PerplexityBot, Google-Extended)
- Root `_site/llms.txt` (from handoff reference, as-is)
- `_site/sitemap.xml` with 48 URLs
- Slash-slug flattening with `slug-flatten-report.txt` (12 county slash slugs)
- Shared layout: head SEO, OG/Twitter, footer disclaimer, `/check` + `/free-help` links
- **Strict content policy applied:**
  - CMS pages: `Name` h1 + empty Body only (no Meta Description / Key Towns / Category prose)
  - Seven shells: structural title + h1 + CTAs only (no invented “content coming” blurbs)
  - Content moved from `index.html`: `/`, `/check`, `/standards`, `/what-it-costs`, `/contact`
- `/check` client-side screener; sends nothing
- `/contact` three-field shell (Lawmatics deferred)
- `npm run verify` PASS with strict no-invented-copy gates

## Placeholder / deferred (for Jaxon)

- Body copy for seven shells
- **Empty meta descriptions** on shells (and currently on `/check`, `/standards`, `/what-it-costs`, `/contact` — structural titles only; home uses reference title/description)
- Live Lawmatics embed on `/contact`
- CMS `Body` rich text (empty by design until legal source copy)
- Photos (initials placeholders) — **#1 launch blocker**
- Laura surname / bar number
- Fee figures pending counsel review
- `test_screener.js` not present in this repo — re-run when handoff file is available
- Confirm flattened county URLs vs SEO plan

## SEO flag for Jaxon

County slugs containing `/` were flattened. Confirm this URL plan via `slug-flatten-report.txt`.

## How to build

```bash
cd .worktrees/strict-copy-pipeline
npm install
npm run build
npm run verify
```

Output directory: `_site/` (ready for GitHub Pages). Staging only until told.

## Branch

`feature/strict-copy-pipeline` (from main @ plan/spec restart commits)
